{
  "name": "curvas-idf-chile",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "node scripts/generate-og-images.mjs && node scripts/generate-favicons.mjs",
    "dev": "astro dev",
    "prebuild": "node scripts/generate-og-images.mjs && node scripts/generate-favicons.mjs",
    "build": "astro build && node scripts/prepare-sites.mjs",
    "preview": "astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "@astrojs/markdown-remark": "^7.2.1",
    "@astrojs/sitemap": "3.7.3",
    "astro": "7.1.1",
    "sharp": "0.35.3"
  },
  "devDependencies": {
    "@astrojs/check": "latest",
    "typescript": "latest"
  }
}
